<div class="row">
    <div class="col-lg-4 profile-nav">
        <ul class="nav nav-pills nav-stacked ">
            <li><a href="/users/profile?action=settings">Profile settings</a></li>
            <li><a href="/users/profile?action=ads">Ads</a></li>
            <li><a href="/users/profile?action=pm">Personal messages</a></li>
        </ul>
    </div>
    <div class="col-lg-8 ">

        {{#if default}}
        {{>user/default }}
        {{/if}}
        {{#if settings}}
        {{>user/settings }}
        {{/if}}
        {{#if pm}}
        {{>user/pm }}
        {{/if}}
        {{#if ads}}
        {{>user/ads }}
        {{/if}}


    </div>
</div>